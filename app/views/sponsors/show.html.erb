<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @sponsor.name %>
</p>

<p>
  <strong>Url:</strong>
  <%= @sponsor.url %>
</p>

<p>
  <strong>Logo:</strong>
  <%= @sponsor.logo %>
</p>

<% content_for(:sidebar) do %>
	<h3>Sponsorships</h3>
	<% @events.each { |e| %>
	<li>
		<% e.sponsors.each { |s| %>
			<% if @sponsor.name == s.name %>
				<% url = events_path+"/"+e.id.to_s+"/" %>
				<%= link_to(e.name, url) %>
			<% end %>
		<% } %>
	</li>
	<% } %>
<% end %>

<%= link_to 'Edit', edit_sponsor_path(@sponsor) %> |
<%= link_to 'Back', sponsors_path %>
